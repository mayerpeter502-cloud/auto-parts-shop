**ТЗ для нового ИИ (AutoParts.kz - финальный этап)**

## Что сделано ✅

**Архитектура:**
- Next.js 14 + TypeScript + Tailwind CSS
- localStorage вместо backend (для продажи без БД покупателей)
- Все файлы `.tsx` (НЕ `.js`!)
- Относительные импорты (`../` или `./`, без `@/`)

**Готовые страницы:**
- `/` - главная с подбором по авто (модалка с автокомплитом)
- `/catalog` - каталог с фильтрами (по авто, цене, категории, наличию)
- `/product/[id]` - карточка товара
- `/cart` - корзина
- `/checkout` - оформление заказа (3 шага: контакты, доставка, оплата)
- `/order-success` - успешный заказ
- `/orders` - мои заказы
- `/login` - вход/регистрация
- `/vin-check` - проверка по VIN
- `/admin` - админка (дашборд, товары, заказы)

**Готовый функционал:**
- Добавление/редактирование/удаление товаров в админке
- Корзина с localStorage
- Оформление заказа без регистрации
- Подбор по авто (марка/модель/год) с автокомплитом
- Фильтры в каталоге
- Адаптивность mobile-first

## Что нужно сделать 📝

**Обязательно для продажи:**

1. **Пагинация в каталоге** (`src/app/catalog/page.tsx`)
   - Показывать по 12 товаров
   - Кнопки "Показать еще" или номера страниц

2. **Блок "С этим покупают"** (`src/app/product/[id]/page.tsx`)
   - Показывать 4 похожих товара (по категории)

3. **Отзывы на товар** (`src/app/product/[id]/page.tsx`)
   - Форма добавления отзыва
   - Список отзывов
   - Хранение в localStorage

4. **Демо-данные** (`src/app/lib/api.ts`)
   - Добавить 20-30 реальных товаров (масла, фильтры, тормоза)
   - С фото (эмодзи или placeholder)
   - С реальными ценами

5. **SEO и мета-теги** (`src/app/layout.tsx`)
   - Title, description
   - Favicon

6. **Страницы контента:**
   - `/about` - О нас
   - `/delivery` - Доставка и оплата
   - `/contacts` - Контакты

**Важно для разработчика:**
- Заказчик НЕОПЫТНЫЙ программист
- ВСЕГДА указывать полный путь к файлу: `src/app/...` или `src/components/...`
- ВСЕГДА писать ПОЛНЫЙ код файла, не отрывки
- ВСЕ файлы `.tsx` (НЕ `.js`!)
- Иконки только из `lucide-react`
- Mobile-first адаптивность Tailwind

**Репозиторий:** https://github.com/Lex793/auto-parts-shop

**Бюджет проекта:** ~1500-2000$ (продажа без БД покупателей, localStorage)