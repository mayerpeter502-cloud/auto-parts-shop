**ТЗ для нового ИИ (AutoParts.kz)**

## Что сделано ✅

**Архитектура:**
- Next.js 14 + TypeScript + Tailwind CSS
- localStorage вместо backend
- Все файлы `.tsx` (не `.js`!)
- Относительные импорты (без `@/`)

**Готовые компоненты:**
- `src/contexts/AuthContext.tsx` - авторизация
- `src/contexts/CartContext.tsx` - корзина
- `src/components/Header.tsx` - шапка
- `src/components/Footer.tsx` - подвал
- `src/app/lib/api.ts` - API для localStorage

**Готовые страницы:**
- `/` - главная (поиск, баннер, категории, товары)
- `/catalog` - каталог с фильтрами
- `/product/[id]` - карточка товара
- `/cart` - корзина
- `/orders` - мои заказы
- `/vin-check` - проверка по VIN
- `/admin` - админка (дашборд)
- `/admin/products` - управление товарами (CRUD)
- `/admin/orders` - управление заказами

## Что нужно сделать 📝

**Критические задачи:**

1. **Страница оформления заказа** (`/checkout`)
   - Форма: имя, телефон, email
   - Выбор доставки: самовывоз, курьер, почта
   - Адрес доставки (если курьер/почта)
   - Выбор оплаты: наличные, карта, Kaspi
   - Подтверждение заказа → сохранение в localStorage

2. **Страница успешного заказа** (`/order-success`)
   - Номер заказа
   - Список товаров
   - Кнопка "Вернуться в каталог"

3. **Страница входа/регистрации** (`/login`)
   - Форма входа
   - Форма регистрации
   - Первый пользователь = админ

4. **Каталог** - доработать:
   - Подбор по автомобилю (марка/модель/год)
   - Фильтр по VIN
   - Пагинация

5. **Админка** - доработать:
   - Добавить товар - работает
   - Редактировать товар - проверить
   - Удалить товар - работает
   - Изменение статусов заказов - работает

**Важно:**
- Mobile-first адаптивность
- Иконки только `lucide-react`
- Все импорты относительные (`../` или `./`)
- localStorage для данных

**Репозиторий:** https://github.com/Lex793/auto-parts-shop