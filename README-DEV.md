## 📋 ТЗ ДЛЯ НОВОГО ИИ (AutoParts.kz - Продолжение разработки)

### 🔗 Репозиторий
**GitHub:** https://github.com/Lex793/auto-parts-shop

---

### ✅ ЧТО УЖЕ СДЕЛАНО:

**Страницы (11 шт.):**
- `localhost:3000/` — Главная (баннеры, категории, товары, VIN-поиск)
- `localhost:3000/catalog` — Каталог (фильтры, сортировка, пагинация)
- `localhost:3000/product` — Страница товара (табы, совместимость, "с этим покупают")
- `localhost:3000/cart` — Корзина (3 шага: контакты, доставка, оплата)
- `localhost:3000/profile` — Личный кабинет (история, избранное, адреса)
- `localhost:3000/favorites` — Избранное (localStorage, добавление в корзину)
- `localhost:3000/compare` — Сравнение товаров (таблица, до 4 товаров)
- `localhost:3000/about` — О компании
- `localhost:3000/delivery` — Доставка и оплата
- `localhost:3000/contacts` — Контакты (форма обратной связи)
- `localhost:3000/not-found` — Страница 404

**Компоненты:**
- `Header` — навигация, поиск, счетчики избранного/корзины
- `Footer` — ссылки, контакты, категории
- `Breadcrumbs` — хлебные крошки с авто-генерацией
- `FilterSidebar` — фильтры по цене, брендам, наличию, категориям
- `Pagination` — пагинация с многоточием
- `ProductCard` — карточка товара (избранное, сравнение, корзина)
- `CompareButton` — кнопка добавления в сравнение
- `CompareWidget` — плавающий виджет сравнения
- `SkeletonLoader` — скелетоны загрузки

**Функционал:**
- localStorage: избранное (`autoparts_favorites`), корзина (`autoparts_cart`), сравнение (`autoparts_compare`)
- Поиск с автодополнением (моковые данные)
- Toast-уведомления
- Адаптивный дизайн (mobile-first)
- SEO: sitemap.xml, robots.txt, метатеги

**Технологии:**
- Next.js 14 (App Router)
- Tailwind CSS
- Lucide React (иконки)
- localStorage для состояния

---

### 📁 СТРУКТУРА ПРОЕКТА:
```
src/
├── app/
│   ├── page.js              # Главная
│   ├── catalog/page.js      # Каталог
│   ├── product/page.js      # Товар
│   ├── cart/page.js         # Корзина
│   ├── profile/page.js      # ЛК
│   ├── favorites/page.js    # Избранное
│   ├── compare/page.js      # Сравнение
│   ├── about/page.js        # О компании
│   ├── delivery/page.js     # Доставка
│   ├── contacts/page.js     # Контакты
│   ├── not-found.js         # 404
│   ├── loading.js           # Глобальный лоадер
│   ├── sitemap.js           # SEO sitemap
│   ├── robots.js            # SEO robots
│   └── layout.js            # Корневой layout
├── components/
│   ├── Header.js
│   ├── Footer.js
│   ├── Breadcrumbs.js
│   ├── FilterSidebar.js
│   ├── Pagination.js
│   ├── ProductCard.js
│   ├── CompareButton.js
│   ├── CompareWidget.js
│   └── SkeletonLoader.js
└── hooks/
    └── useFavorites.js      # Хук избранного
```

---

### 🎯 ЧТО НУЖНО СДЕЛАТЬ:

**1. Интеграция с бэкендом:**
- Заменить моковые данные на API-запросы
- Подключить реальный поиск с бэкендом
- Интеграция VIN-запроса с реальным API

**2. Корзина (доработка):**
- Синхронизация корзины с localStorage на всех страницах
- Добавить изменение количества товаров
- Удаление товаров из корзины
- Промокоды и скидки

**3. Личный кабинет:**
- Интеграция с API авторизации
- Редактирование профиля
- Загрузка аватара
- История заказов с сервера

**4. Оформление заказа:**
- Валидация форм
- Интеграция платежей (Kaspi Pay и др.)
- Отправка заказа на бэкенд
- Email-уведомления

**5. Дополнительные страницы:**
- `/blog` — Статьи о ремонте и обслуживании
- `/brands` — Список брендов
- `/vin` — Отдельная страница VIN-подбора
- `/search` — Результаты поиска

**6. Улучшения UX:**
- Быстрый просмотр товара (модалка)
- Липкий хедер при скролле
- Ленивая загрузка изображений
- Оптимизация производительности

**7. Админ-панель (опционально):**
- Управление товарами
- Управление заказами
- Статистика продаж

---

### ⚠️ ВАЖНЫЕ ПРАВИЛА РАБОТЫ:

1. **Mobile-first** Tailwind CSS — все стили сначала для мобильных
2. **Компоненты** создавать в `src/components/`
3. **НЕ переписывать** существующие страницы полностью — только дополнять
4. **Иконки** — только `lucide-react`
5. **localStorage ключи:**
   - `autoparts_favorites` — избранное
   - `autoparts_cart` — корзина
   - `autoparts_compare` — сравнение
6. **Git:** коммиты через CMD, сообщения на английском с префиксом `feat:`, `fix:`, `refactor:`
7. **Проверка:** перед каждым новым кодом — `git status` и `git pull`

---

### 🎨 ДИЗАЙН-РЕФЕРЕНС:
Визуально как **Flip.kz** — чистый белый фон, синие акценты (`blue-600`), карточки с тенями, простая типографика.

---

### 🔧 РАБОЧИЙ ПРОЦЕСС:

Пользователь работает в **2 окнах CMD**:
- **Окно 1:** `npm run dev` (запущен Next.js)
- **Окно 2:** сохранение файлов и git-команды

**Команды для CMD (окно 2):**
```bash
cd D:\auto-parts
git status
git add .
git commit -m "feat: описание изменений"
git push origin main
git log --oneline -3
```

---

### 📋 ТРЕБОВАНИЯ К НОВОМУ ИИ:

1. **Проверяй контекст** — изучи структуру проекта перед кодом
2. **Предупреждай о токенах** при ~90% использования
3. **Предоставляй полный код** файлов, не обрывай посередине
4. **Указывай путь файла** для каждого блока кода
5. **Git-команды отдельно** — пользователь копирует в CMD
6. **Не предполагай** наличие файлов — проверяй или создавай
7. **Работай поэтапно** — одна фича = один коммит

