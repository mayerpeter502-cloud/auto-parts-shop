**Лимит токенов: ~8%** — создаю ТЗ для нового ИИ.

---

## 📋 ТЗ ДЛЯ НОВОГО ИИ (AutoParts.kz — Продолжение)

**Репозиторий:** https://github.com/Lex793/auto-parts-shop

---

### ✅ ЧТО УЖЕ СДЕЛАНО:

**Авторизация:**
- Локальная auth через localStorage (без Firebase)
- Админ-панель с защитой по email
- Быстрый вход для теста

**Админ-панель:**
- Дашборд с статистикой (мок-данные)
- Список товаров с таблицей
- Добавление нового товара
- Удаление товаров
- Сохранение в localStorage

**API:**
- `productApi` — CRUD на localStorage
- `useAdmin` hook для проверки прав
- Типы данных в `types/index.ts`

---

### 🎯 ЧТО НУЖНО СДЕЛАТЬ:

**1. Доработка админ-панели**
- Редактирование товара (кнопка ✏️ сейчас не работает)
- Страница заказов (список, статусы)
- Загрузка изображений (file input + base64)

**2. Интеграция с фронтендом**
- Вывод реальных товаров на главной
- Каталог с фильтрами (марка, модель, категория)
- Карточка товара с характеристиками

**3. Корзина и оформление**
- Корзина в localStorage
- Оформление заказа (3 шага: контакты → доставка → подтверждение)
- История заказов в админке

**4. Поиск и фильтры**
- Поиск по названию/артикулу
- Подбор по VIN (мок)
- Подбор по автомобилю (марка/модель/год)

**5. SEO и финал**
- Мета-теги для всех страниц
- Sitemap
- Инструкция по деплою

---

### 📁 СТРУКТУРА ПРОЕКТА:

```
src/
├── app/
│   ├── admin/          # Админ-панель (готово)
│   ├── login/          # Страница входа (готово)
│   ├── test/           # Тестовая страница
│   └── ...             # Остальные страницы
├── components/         # UI компоненты
├── contexts/
│   └── AuthContext.tsx # Авторизация (готово)
├── hooks/
│   └── useAdmin.ts     # Проверка админа (готово)
├── lib/
│   ├── api/
│   │   ├── products.ts # API товаров (готово)
│   │   └── orders.ts   # API заказов (пусто)
│   └── firebase.ts     # Отключен, используем localStorage
└── types/
    └── index.ts        # Типы (готово)
```

---

### ⚠️ ВАЖНЫЕ ПРАВИЛА:

1. **БЕЗ реальных данных покупателей** — только тестовые данные
2. **localStorage вместо Firebase** — пока не настроен backend
3. **Mobile-first** Tailwind
4. **Иконки** — только `lucide-react`
5. **Git:** коммиты с префиксами `feat:`, `fix:`, `refactor:`

---

### 🎨 ДИЗАЙН:
Как Flip.kz — белый фон, синие акценты (`blue-600`), чистые карточки.

---

**Пользователь работает в 2 окнах CMD:**
- Окно 1: `npm run dev`
- Окно 2: git-команды

---

