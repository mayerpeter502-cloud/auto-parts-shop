Создаю ТЗ для нового ИИ:

---

## ТЗ для нового ИИ (AutoParts.kz)

### ✅ ЧТО УЖЕ СДЕЛАНО:

**Архитектура:**
- Next.js 14 + Tailwind CSS + TypeScript
- localStorage вместо backend
- Mobile-first адаптивность

**Компоненты:**
- `Header` — с навигацией, поиском, индикатором корзины
- `Footer` — с контактами, ссылками, соцсетями
- `AuthProvider` — контекст авторизации (admin@autoparts.kz / admin123)

**Хуки:**
- `useCart.ts` — корзина с localStorage, проверками на undefined
- `useAuth.ts` — авторизация

**Страницы созданы:**
- `/` — главная (Flip-стиль)
- `/catalog` — каталог с фильтрами (есть ошибка импорта api.ts)
- `/product/[id]` — карточка товара
- `/cart` — корзина
- `/checkout` — оформление заказа (3 шага)
- `/order-success` — успешный заказ
- `/vin-check` — проверка по VIN
- `/car-selector` — подбор по авто
- `/login` — вход в админку

**Админка (начата):**
- `/admin` — дашборд
- `/admin/products` — список товаров
- `/admin/orders` — заказы

---

### 🎯 ЧТО НУЖНО СДЕЛАТЬ:

**1. Исправить ошибки:**
- Создать `/app/lib/api.ts` — API для работы с localStorage (Product тип, productApi)
- Проверить все импорты `@/app/...` → заменить на относительные `../` где нужно

**2. Доработать админку:**
- Подключить реальные данные заказов из localStorage
- CRUD операции для товаров
- Управление статусами заказов

**3. Личный кабинет (опционально):**
- `/profile` — история заказов
- `/favorites` — избранное
- `/addresses` — адреса доставки

**4. Дополнительно:**
- Страницы: `/delivery`, `/warranty`, `/contacts`, `/privacy`, `/terms`
- Обработка пустых состояний (нет товаров, пустая корзина)
- Улучшить SEO мета-теги
- Добавить toast-уведомления вместо alert()

---

### 📁 СТРУКТУРА ПРОЕКТА:
```
src/app/
├── components/     # Header, Footer
├── contexts/       # AuthContext
├── hooks/          # useCart, useAuth
├── lib/            # api.ts (НЕТ ФАЙЛА!)
├── admin/          # Страницы админки
├── product/[id]/   # Карточка товара
├── cart/           # Корзина
├── checkout/       # Оформление заказа
├── vin-check/      # VIN проверка
├── car-selector/   # Подбор по авто
├── login/          # Вход
└── page.tsx        # Главная
```

---

### ⚠️ ВАЖНЫЕ ПРАВИЛА:
- **localStorage только** — нет backend/Firebase
- **Иконки** — только `lucide-react`
- **Mobile-first** Tailwind
- **Git коммиты** с префиксами `feat:`, `fix:`

### 🎨 ДИЗАЙН:
Как Flip.kz — белый фон, синие акценты (`blue-600`), чистые карточки.

**Репозиторий:** https://github.com/Lex793/auto-parts-shop