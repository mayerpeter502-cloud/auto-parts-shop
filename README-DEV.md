Вот ТЗ для нового ИИ:

---

## 📋 ТЗ ДЛЯ НОВОГО ЧАТА: AutoParts.kz

### 🔗 Ссылка на репозиторий
**GitHub:** https://github.com/Lex793/auto-parts-shop

---

### ✅ ЧТО УЖЕ СДЕЛАНО:

**Страницы (5 шт.):**
- `localhost:3000/` — Главная (полная: баннеры, категории, товары, VIN-поиск)
- `localhost:3000/catalog` — Каталог (фильтры, сортировка, карточки)
- `localhost:3000/product` — Страница товара (фото, характеристики, таблица совместимости)
- `localhost:3000/cart` — Корзина (3 шага: контакты, доставка, оплата)
- `localhost:3000/profile` — Личный кабинет (история, избранное, адреса)

**Функционал:**
- Поиск с автодополнением (моковые данные)
- Избранное с localStorage (сердечко у товаров)
- Toast-уведомления "Добавлено в корзину"
- Карусель баннеров на главной
- Подбор по VIN (форма + моковый ответ)
- Таблица совместимости авто на странице товара
- Блок "С этим покупают"

**Технологии:**
- Next.js 14 (App Router)
- Tailwind CSS (mobile-first)
- Lucide React (иконки)
- localStorage для избранного

---

### 📁 СТРУКТУРА ПРОЕКТА (уже создано):
```
src/
├── app/
│   ├── page.js              # Главная
│   ├── catalog/
│   │   └── page.js          # Каталог
│   ├── product/
│   │   └── page.js          # Товар (с избранным)
│   ├── cart/
│   │   └── page.js          # Корзина
│   ├── profile/
│   │   └── page.js          # ЛК
│   └── layout.js            # Корневой layout
├── hooks/
│   └── useFavorites.js      # Хук для избранного
└── components/              # Пока пусто, создавать здесь
```

---

### 🎯 ЧТО МОЖНО СДЕЛАТЬ БЕЗ ЗАКАЗЧИКА:

1. **Страница "Избранное"** (`/favorites`)
   - Отображение товаров из localStorage
   - Кнопка "Добавить все в корзину"
   - Удаление из избранного

2. **Сравнение товаров**
   - Добавить кнопку "Сравнить" у товаров
   - Страница `/compare` с таблицей сравнения

3. **Фильтры каталога (расширить)**
   - Фильтр по цене (range slider)
   - Фильтр по наличию
   - Фильтр по брендам (чекбоксы)

4. **Анимации и UX**
   - Skeleton loaders при загрузке
   - Анимация добавления в корзину
   - Плавные переходы между страницами

5. **SEO и метаданные**
   - meta-теги для всех страниц
   - Open Graph для соцсетей
   - sitemap.xml

6. **Оптимизация**
   - Lazy loading для изображений
   - Оптимизация шрифтов
   - PWA (manifest, service worker)

7. **Дополнительные страницы**
   - `/about` — О компании
   - `/delivery` — Доставка и оплата
   - `/contacts` — Контакты
   - `/blog` — Статьи (моковые)

8. **Улучшение существующего**
   - Пагинация в каталоге
   - Быстрый просмотр товара (модалка)
   - Хлебные крошки на всех страницах
   - Фильтры в URL (query params)

---

### ⚠️ ВАЖНЫЕ ПРАВИЛА:
- **Mobile-first** Tailwind CSS
- Новые компоненты в `src/components/`
- **НЕ переписывать** существующие страницы — только дополнять
- Использовать **lucide-react** для иконок
- localStorage ключ: `autoparts_favorites`

---

### 🎨 ДИЗАЙН-РЕФЕРЕНС:
Визуально как **Flip.kz** — чистый, белый фон, синие акценты (`blue-600`), карточки с тенями, простая типографика.

