# ТЗ для нового ИИ (AutoParts.kz)

## Основные требования
- Next.js 14 + Tailwind CSS + TypeScript
- localStorage вместо backend
- Mobile-first адаптивность (Tailwind)
- Иконки только из lucide-react
- Git коммиты с префиксами (feat:, fix:)

## Что уже сделано
- Архитектура проекта
- Header, Footer, AuthProvider
- useCart, useAuth хуки
- Основные страницы: главная, каталог, карточка товара, корзина, оформление заказа
- Начата админка (/admin, /admin/products, /admin/orders)

## Критические задачи
1. Создать `src/app/lib/api.ts` (API для работы с localStorage)
2. Исправить импорты (заменить `@/app/...` на относительные `../`)
3. Доработать админку:
   - Подключить данные заказов из localStorage
   - Реализовать CRUD операции для товаров
   - Добавить управление статусами заказов

## Дизайн
- Белый фон, синие акценты (blue-600)
- Чистые карточки как на Flip.kz
- Обязательные элементы:
  * Большой поисковой блок на главной
  * Карусель баннеров
  * Популярные категории
  * "Подбор по автомобилю" и "Подбор по VIN"
  * Фильтры в каталоге по авто-параметрам

## Правила работы
- Все команды для CMD писать полностью
- Сохранять адаптивность (mobile-first)
- Выводить полные коды, не отрывки
- Сохранять изменения через git add/commit/push
- Предупреждать о лимите токенов при достижении 95%

## Рекомендуемая модель ИИ
Выберите **Qwen-Max** - у нее наибольший лимит токенов в чате (до 32768 токенов), что критично для сложных проектов с длинными диалогами и большим количеством кода. Она лучше всего подходит для продолжения разработки, так как позволяет обрабатывать большие контексты и сохранять историю обсуждения проекта.

**Репозиторий:** https://github.com/Lex793/auto-parts-shop
Обновленное ТЗ для нового ИИ (AutoParts.kz)
🔧 Критические ошибки, которые нужно исправить
Ошибка импорта контекстов
Module not found: Can't resolve '@/contexts/CartContext'
Нужно заменить все импорты с @/contexts/... на относительные пути
Ошибка useAuth
useAuth must be used within AuthProvider
Нужно правильно обернуть компоненты в AuthProvider
✅ Что уже сделано
Создан src/app/lib/api.ts (API для работы с localStorage)
Установлены зависимости: uuid, react-hot-toast
Добавлен ToastProvider
Созданы базовые страницы и компоненты
Настроена архитектура проекта
📝 Правила для нового ИИ
Используйте Qwen-Max - максимальный лимит токенов
Все команды для CMD должны быть полными
Сохраняйте адаптивность (mobile-first)
Выводите полные коды, а не отрывки
Предупреждайте о лимите токенов при достижении 95%
Проверяйте импорты и структуру проекта
⚠️ Важно!
Все импорты должны быть относительными (без @/)
Убедитесь, что файлы контекстов существуют в src/contexts/
Перезапустите dev-сервер после изменений